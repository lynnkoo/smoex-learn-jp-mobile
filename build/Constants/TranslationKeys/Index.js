import Test from './Test';
import SearchPanel from './SearchPanel';
import Page from './Page';
export { Test, Page };
export default Object.assign(Object.assign(Object.assign({}, Test), SearchPanel), Page);

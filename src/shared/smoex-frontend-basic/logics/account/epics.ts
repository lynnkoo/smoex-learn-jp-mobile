import { combineEpics, Epic } from 'redux-observable'

// const connectRoomEpic: Epic<any, any> = (action$) =>
//   action$.ofType(ROOM_GET_INFO_SUCCESS)
//     .map(() => actionCreators.connectRoom())

// export const epics = combineEpics(
